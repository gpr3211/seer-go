## A basic Go function extractor.

## Usage
## Install
    ```bash
         go install -o <name>
    ```

## Usage
 ```
    ./<name> -dir <dir_path>(relative)
```
    - To parse a single dir. If no flag will parse go files in entire curr dir tree.
    
